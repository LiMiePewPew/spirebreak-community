# Optional. The public site works without a token.
# Set only in the Cloudflare build environment if GitHub's anonymous build-time
# API limit ever becomes too small. Never prefix this with PUBLIC_.
GITHUB_TOKEN=
