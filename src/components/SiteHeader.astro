---
const path = Astro.url.pathname;
const nav = [
  ['/#evolution', 'The game'],
  ['/changelog', 'Updates'],
  ['/roadmap', "What's next"],
];
---
<header class="site-header">
  <div class="header-inner shell">
    <a class="brand" href="/" aria-label="Spirebreak home">
      <img class="brand-icon" src="/media/brand/spirebreak-mark.png" alt="" width="96" height="96" />
      <img class="brand-wordmark" src="/media/brand/spirebreak-wordmark.webp" alt="" width="1019" height="79" />
    </a>
    <nav aria-label="Primary navigation">
      {nav.map(([href, label]) => (
        <a href={href} aria-current={path.startsWith(href) ? 'page' : undefined}>{label}</a>
      ))}
    </nav>
    <a class="nav-status" href="/development"><i></i> In development</a>
  </div>
</header>
