---
import StatusPill from './StatusPill.astro';
interface Props {
  title: string;
  area: string;
  summary: string;
}
const { title, area, summary } = Astro.props;
---
<article class="roadmap-card">
  <div class="card-meta">
    <StatusPill text={area.toUpperCase()} tone="bronze" />
  </div>
  <h3>{title}</h3>
  <p>{summary}</p>
</article>
